import { urls } from '../http';

const INDEX_PRODUCT_IMAGE_404_NAME = 'no-image.png';

const obj = {
    INDEX_PRODUCT_IMAGE_404_NAME,
    IMG_NO_IMG_URL: urls.apiSysImagesBasePathURL + INDEX_PRODUCT_IMAGE_404_NAME
};

export default obj;